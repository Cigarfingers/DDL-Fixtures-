import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

### **6. Create `src/App.jsx`:**
Copy your updated React component code (the one without the leg badges) into this file.

### **7. Create `.gitignore`:**
```
node_modules
dist
.DS_Store
